"use client";

import styled from "styled-components";

const StyledHeader = styled.header`
  text-align: center;
  padding: 24px 0;
`;

export const Header = () => {
  return <StyledHeader>STATIC HEADER COMPONENT</StyledHeader>;
};
